# Mouse support: click to focus panes, drag borders to resize, wheel to scroll
set -g mouse on

# Pass modified keys (Shift+Enter etc.) through to TUIs — pi warns without this
set -g extended-keys on
